root := ..
subdirs := 
pre-targets := 
bin := yes
targets := keytest mgrtest parsekey listtest casttest modtest compnone complzss compzip
include ${root}/Makefile.cfg

ifneq ($(nodeps),yes)
-include $(d)
endif
